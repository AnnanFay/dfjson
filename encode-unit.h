Value encode();
Value encode(T_body_part_temperature*);
Value encode(T_inventory*);
Value encode(T_unk5*);
Value encode(T_unk_5b0*);
Value encode([string]);
Value encode(body_part_raw*);
Value encode(bool);
Value encode(bool[]);
Value encode(building*);
Value encode(df::::T_pickup_flags);
Value encode(df::enum_field<T_id, int16_t>);
Value encode(df::enum_field<T_soldier_mood, int16_t>);
Value encode(df::enum_field<T_type, int16_t>);
Value encode(df::enum_field<item_type, int16_t>);
Value encode(df::enum_field<job_skill, int16_t>);
Value encode(df::enum_field<profession, int16_t>);
Value encode(df::language_name);
Value encode(df::unit);
Value encode(df::unit::T_body);
Value encode(df::unit::T_counters);
Value encode(df::unit::T_job);
Value encode(df::unit::T_last_hit);
Value encode(df::unit::T_military);
Value encode(df::unit::T_path);
Value encode(df::unit::T_relations);
Value encode(df::unit::T_status);
Value encode(df::unit::T_status2);
Value encode(df::unit::T_unknown1);
Value encode(df::unit::T_unknown2);
Value encode(df::unit::T_unknown3);
Value encode(df::unit::T_unknown4);
Value encode(df::unit::T_unknown6);
Value encode(df::unit::T_unknown8);
Value encode(df::unit_attribute);
Value encode(df::unit_flags1);
Value encode(df::unit_flags2);
Value encode(df::unit_flags3);
Value encode(df::unit_misc_trait);
Value encode(df::unit_preference);
Value encode(df::unit_preference::T_);
Value encode(df::unit_skill);
Value encode(df::unit_soul);
Value encode(general_ref*);
Value encode(int16_t);
Value encode(int32_t);
Value encode(int8_t);
Value encode(item*);
Value encode(job*);
Value encode(meeting_ref*);
Value encode(std::string);
Value encode(std::vector<T_body_part_temperature*>);
Value encode(std::vector<T_inventory*>);
Value encode(std::vector<T_unk5*>);
Value encode(std::vector<body_part_raw*>);
Value encode(std::vector<body_part_raw*>*);
Value encode(std::vector<building*>);
Value encode(std::vector<general_ref*>);
Value encode(std::vector<int16_t>);
Value encode(std::vector<int32_t>);
Value encode(std::vector<int32_t>[]);
Value encode(std::vector<meeting_ref*>);
Value encode(std::vector<pointer>);
Value encode(std::vector<uint16_t>);
Value encode(std::vector<uint32_t>);
Value encode(std::vector<uint8_t>);
Value encode(std::vector<unit_misc_trait*>);
Value encode(std::vector<unit_preference*>);
Value encode(std::vector<unit_skill*>);
Value encode(std::vector<unit_soul*>);
Value encode(std::vector<void>);
Value encode(uint16_t);
Value encode(uint16_t[]);
Value encode(uint32_t);
Value encode(uint8_t);
Value encode(unit*);
Value encode(unit_attribute[]);
Value encode(unit_misc_trait*);
Value encode(unit_preference*);
Value encode(unit_skill*);
Value encode(unit_soul*);
Value encode(void*);
